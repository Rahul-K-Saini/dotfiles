#!/usr/bin/env bash

choice=$(printf "Lock\nLogout\nReboot\nShutdown" | rofi -dmenu -p "Power")

case "$choice" in
  Lock) ~/.local/bin/lock ;;
  Logout) i3-msg exit ;;
  Reboot) systemctl reboot ;;
  Shutdown) systemctl poweroff ;;
esac
